# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# switch panes using Alt-arrow without prefix
bind -n M-^ last-window
bind -n M-k select-pane -U
bind -n M-j select-pane -D
bind -n M-h select-pane -L
bind -n M-l select-pane -R

# reload config file (change file location to your the tmux.conf you want to use)
bind r source-file ~/.config/tmux/tmux.conf

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# don't rename windows automatically
set-option -g allow-rename off


# needed by nvim
set-option -g focus-events on
######################
### DESIGN CHANGES ###
######################

# Check Colors
##########################################
# for i in {0..255} ; do
#    printf "\x1b[38;5;${i}mcolour${i}\n"
# done
##########################################

# loud or quiet?
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

#  modes
# setw -g clock-mode-colour colour5
# setw -g mode-style 'fg=colour0 bg=colour129'
#
# # panes
# set -g pane-border-style 'fg=colour171 bg=colour0'
# set -g pane-active-border-style 'bg=colour0 fg=colour171'
#
# # statusbar
# set -g status-position bottom
# set -g status-justify left
# set -g status-style 'bg=colour171 fg=colour171 dim'
# set -g status-left ''
# set -g status-right '#[fg=colour0,bg=colour129] %d/%m #[fg=colour0,bg=colour129] %H:%M:%S '
# set -g status-right-length 50
# set -g status-left-length 20
#
# setw -g window-status-current-style 'fg=colour0 bg=colour129'
# setw -g window-status-current-format ' #I#[fg=colour0]:#[fg=colour0]#W#[fg=colour0]#F '
#
# setw -g window-status-style 'fg=colour0 bg=colour171'
# setw -g window-status-format ' #I#[fg=colour0]:#[fg=colour0]#W#[fg=colour0]#F '
#
# setw -g window-status-bell-style 'fg=colour0 bg=colour1 bold'
#
# # messages
# set -g message-style 'fg=colour0 bg=colour16 bold'
#

# dracula
set -g @dracula-plugins "battery cpu-usage ram-usage time"
set -g @dracula-show-powerline true
set -g @dracula-day-month true
set -g @dracula-show-timezone false
set -g @dracula-show-left-icon "🐧"
set -g @dracula-cpu-usage-label ""
set -g @dracula-ram-usage-label ""

run ~/.config/dotfiles/tmux/dracula/scripts/dracula.sh
